use_default_settings: true

server:
  secret_key: "perplexity-from-scratch-dev-only-not-a-secret"
  limiter: false
  image_proxy: false

search:
  formats:
    - html
    - json

general:
  instance_name: "perplexity-clone-searxng"
